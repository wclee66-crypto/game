# 새록 (saerok)

> 이름은 2026-08-21 에 **맑은뜰 → 새록** 으로 바뀌었습니다 (`4cc04df`).
> '기억이 새록새록'에서 온 이름이고, 아이콘은 새싹 잎 셋입니다.
> 화면·문서에는 반드시 **새록**을 씁니다. 다만 저장 키(`malgeunddeul.v1`)와
> 배포 주소는 **saerokapp.netlify.app** 입니다.
> 저장 키(`malgeunddeul.v1`)만은 **그대로 두어야 합니다.** 바꾸면 기존 점수 기록이 사라집니다.

## 이 프로젝트가 무엇인가

**치매 예방과 인지 능력 향상을 목적으로 하는 두뇌 훈련 게임 웹사이트**입니다.
어르신(고령자)과 경도 인지 장애·치매 환자가 매일 조금씩 놀이처럼 머리를 쓰도록 돕는 것이 유일한 목적이며,
게임의 재미보다 **꾸준히 할 수 있는가 · 혼자서도 할 수 있는가**가 항상 우선입니다.

- 주 사용자: 어르신 본인, 그리고 곁에서 도와주는 보호자·요양보호사
- 하는 일: 스도쿠 · 낱말찾기 · 숫자 계산 · 단어 순서 · 상식 퀴즈 · 색칠 공부 · 틀린그림찾기 (일곱 가지 · 계속 늘어남)
  를 하루 단위로 즐기고, 날짜별 점수가 자동으로 기록됨
- 기록이 남는 이유도 경쟁이 아니라 **꾸준함을 눈으로 확인하고 인지 상태의 변화를 살펴보기 위함**
- 서버·계정 없는 정적 PWA. 설치하면 인터넷 없이도 실행됨 (배포: https://saerokapp.netlify.app)

## 어디로 가려고 하는가 — 이 판단이 모든 작업에 걸린다

**한국에만 머무르지 않고 영어판을 만들어 전 세계 사용자를 모으는 것이 목표입니다.**
**쓰는 것은 언제나 공짜이고, 방문자를 늘려 구글 등의 광고로 수익을 냅니다.**

그래서 무엇을 만들든 아래 셋을 함께 따진다. 어느 하나라도 어긋나면 시작하기 전에 말한다.

1. **모든 문구는 `T()` 로 감싼다.** (2026-08-21 에 갖췄다)
   화면에 나오는 글은 반드시 `T('한국어 문구')` 로 적는다. 그냥 적으면 영어판에서 한국어가 나온다.
   - 한국어 문구 자체가 열쇠다. 번역이 없으면 한국어가 그대로 나오므로 화면이 비지 않는다.
   - 숫자·이름이 끼어들면 이어 붙이지 말고 자리를 판다 —
     ✕ `'오늘 ' + n + '종목'`   ○ `T('오늘 {n}종목', { n: n })`
     말마다 어순이 달라, 토막 내면 옮길 수 없다.
   - 영어는 `js/lang/en.js` 에 `{ '한국어': 'English' }` 로 채운다.
   - 한글 글자 수에 기대어 칸 너비를 정하지 않는다. 영어는 훨씬 길어진다.
   - **말이 필요 없는 게임을 우선한다** — 스도쿠·숫자 계산·색칠 공부·틀린그림찾기는
     문구만 바꾸면 그대로 세계에서 통한다. 낱말찾기·단어 순서는 나라마다
     문제 은행을 새로 만들어야 하므로 품이 몇 배로 든다.
   - 상식 퀴즈는 2026-08-21 에 **영어 문제 은행**(`js/data/quiz-data-en.js`, 139문항)을 갖춰
     두 말에서 모두 나온다. 문제 은행을 말별로 갈아 끼우는 방식(`bank()`)이라
     새 말을 더할 때도 같은 자리에 한 줄만 더하면 된다.

2. **사람이 찾아올 수 있는가.** (2026-08-22 에 갖췄다)
   광고 수익은 방문자 수에서 나오고, 방문자는 대부분 검색으로 온다.
   앱 자체는 화면 하나짜리라 검색에 안 걸리므로, **글이 있는 낱장 페이지를 따로 만들어 둔다.**
   - `tools/build-seo.js` 가 게임 파일을 읽어 페이지를 **자동으로** 만든다.
     `/sudoku/` `/print/` `/en/sudoku/` … 그리고 `sitemap.xml` `robots.txt`.
     **게임을 더하면 페이지도 저절로 늘어난다. 직접 만들지 않는다.**
   - `pack.cmd` 가 배포 파일을 만들 때 이 생성기를 먼저 돌린다.
   - 낱장 페이지의 글(소개·문제지 안내)은 `tools/build-seo.js` 의 `COPY` 에 있다.
   - 가장 값어치 있는 자리는 **`/print/` (무료 인쇄 활동지)** 다.
     요양보호사·복지관이 매일 찾는 것이라 검색 수요가 크다. 이 페이지의 글을 함부로 줄이지 않는다.
   - **문제지 그림이 곧 들어오는 길이다** (2026-08-22 에 갖췄다).
     사람들은 '치매 어르신 문제지'를 **이미지 검색**으로 찾아, 그림을 보고 홈페이지로 들어온다.
     그래서 진짜 문제지를 그림으로 찍어 `images/` 에 두고 페이지에 붙인다.
     - `tools/build-images.js` 가 크롬을 화면 없이 돌려 PNG 로 찍는다. `pack.cmd` 가 함께 돌린다.
       (크롬이 없으면 그냥 건너뛰므로 배포가 멈추지 않는다)
     - 그림 설명(`alt`)이 사실상 검색어다. `shotAlt()` 한 곳에서 만든다.
       ✕ '스도쿠 그림'   ○ '스도쿠 무료 인쇄 문제지 — 어르신 치매 예방 두뇌 훈련 활동지 (A4 한 장)'
     - `sitemap.xml` 에 그림 목록(`image:image`)도 함께 적는다. 이미지 검색에 훨씬 빨리 걸린다.
     - 카톡·페이스북에 주소를 보낼 때 나올 가로 그림(1200×630)도 같은 도구가 만든다.
       카톡은 **가로 그림만** 크게 보여 주므로, 세로 문제지를 그대로 쓰면 안 된다.
   - **누가 얼마나 오는지 세는 것** — `js/analytics.js` (구글 애널리틱스 GA4).
     측정 ID(`G-…`)를 그 파일 맨 위에 넣어야 켜진다.
     비어 있으면 아무 일도 하지 않고, 내 컴퓨터(localhost)에서는 세지 않는다.
     Cloudflare Web Analytics 를 먼저 대 보았으나 안 됐다 — 우리 도메인은
     회색 구름(DNS only)이라 Cloudflare 를 거치지 않고, 그쪽은 자기 도메인이
     아니면 열쇠를 내주지 않는다. 이 판단을 되풀이하지 않는다.
     **구글 것은 쿠키를 쓴다.** 광고를 붙이는 날 개인정보 안내와 동의 창을 함께 만든다.

3. **광고가 들어갈 자리가 있는가 — 그러나 어르신을 방해하지 않는가.**
   아직 광고를 붙이지 않았다. 붙일 때는 이 순서를 지킨다.
   - **게임을 푸는 도중에는 절대 넣지 않는다.** 집중을 끊으면 이 앱의 존재 이유가 무너진다.
   - 넣을 자리는 홈 아래쪽 · 결과 화면 · 게임 고르기 화면처럼 **손을 멈추는 순간**이다.
   - 잘못 눌리기 쉬운 곳(단추 바로 옆·아래)에 두지 않는다.
   - 인쇄 문제지에는 광고가 절대 나오지 않게 한다 (`@media print` 에서 숨긴다).
   - 계정이 없고 기록은 이 기기에만 있다. 광고를 붙이면 유럽·미국의 동의 절차가 필요해지므로,
     그때 개인정보 안내와 동의 창을 함께 만든다.

이 셋은 '나중에 할 일'이 아니라 **지금 만드는 것이 나중에 발목을 잡지 않게 하는 기준**이다.
당장 영어판이나 광고를 만들라는 뜻이 아니다.

### 서버를 두지 않는다 — 아직은

지금 이 앱의 큰 장점은 **서버가 없다는 것**이다. 고장 날 것이 없고, 돈이 안 들고,
인터넷이 끊겨도 열린다. 로그인·게시판처럼 서버가 필요한 것은 신중하게 판단한다.

- **건의 받기**는 서버 없이 한다 — `js/suggest.js` 가 호스팅(Netlify)의 받는 곳으로 바로 보낸다.
  `index.html` 의 숨은 `<form name="suggest">` 를 배포할 때 호스팅이 읽어 자리를 만든다.
  이 form 을 지우면 건의가 들어오지 않는다.
- **게시판·로그인**은 사람이 꾸준히 모인 뒤에 판단한다. 지금 만들면 빈 게시판이 남고,
  스팸·관리·개인정보 책임만 늘어난다. 로그인을 만든다면 그때 가장 값어치 있는 쓸모는
  게시판이 아니라 **점수를 기기끼리 이어 보기**다.

## 함께 일하는 사람 — 답변할 때 반드시 지킬 것

이 저장소의 주인은 **60세 남성이며, 코딩과 개발에 대한 지식이 전혀 없습니다.**
코드를 읽거나 고칠 줄 모르고, 개발 용어도 모릅니다. 그래서 답변은 아래를 항상 지킵니다.

- **모든 답변은 한국어 존댓말**로, 쉬운 일상 표현으로 씁니다.
- **개발 용어를 그냥 쓰지 않습니다.** 꼭 필요하면 한 번은 쉬운 말로 풀어 줍니다.
  (예: "리팩터링" ✕ → "겉으로 보이는 동작은 그대로 두고 속을 정리했습니다" ○,
   "커밋" → "지금까지 고친 내용을 저장했습니다", "캐시" → "기기에 저장해 둔 파일")
- **무엇이 달라졌는지를 먼저 말합니다.** 코드가 어떻게 생겼는지가 아니라
  **화면에서 무엇이 어떻게 보이고 어떻게 동작하는지**를 기준으로 설명합니다.
- 코드 조각은 꼭 필요할 때만 조금 보여 주고, 길게 늘어놓지 않습니다.
- 확인해 달라고 부탁할 때는 **누를 버튼과 순서를 하나씩** 알려 줍니다.
  ("~를 테스트해 주세요" ✕ → "주소창에 ○○를 열고, 아래쪽 △△ 단추를 눌러 보세요" ○)
- 선택지를 드릴 때는 **추천 하나를 먼저 정해서** 말씀드리고, 왜 그것이 나은지 한 줄로 덧붙입니다.
  여러 방법을 늘어놓고 고르라고 하지 않습니다.
- 잘 모르시는 것을 여쭤보셔도 당연한 질문으로 대합니다. 설명은 짧게, 그러나 빠뜨림 없이.

## 이 목적에서 나오는 규칙 (기능을 더하거나 고칠 때 반드시 지킬 것)

1. **큰 글씨, 큰 버튼, 높은 명암비.** 터치 대상은 넉넉하게. 설정의 글씨 크기(보통/크게/아주 크게)를 항상 견뎌야 한다.
2. **모든 문구는 한국어 존댓말**로, 짧고 쉬운 말을 쓴다. 외래어·전문 용어·영어 약어를 화면에 노출하지 않는다.
   (예: "세션 복원" ✕ → "이어서 하기" ○)
3. **실패해도 벌주지 않는다.** 틀려도 점수를 깎아 좌절시키지 않고, 그만두어도 그때까지의 점수는 남긴다.
   시간 제한은 압박이 아니라 보너스의 근거로만 쓴다.
4. **한 화면에 한 가지 일.** 숨은 제스처, 복잡한 메뉴, 여러 단계를 거치는 흐름을 만들지 않는다.
5. **기록은 지워지면 안 된다.** 점수는 이 기기의 localStorage에만 있다. 갱신·초기화 기능을 손댈 때
   기록이 날아갈 여지가 있는지 반드시 확인한다. (내보내기/가져오기가 유일한 백업 수단)
6. **인터넷 없이도 열려야 한다.** 파일을 고치면 `sw.js` 의 `VERSION` 과 `js/app.js` 의 `APP_VERSION` 을
   함께 올리고, 새 파일은 `sw.js` 의 `ASSETS` 목록에 넣는다.
7. **인쇄(A4) 지원은 중요한 기능이다.** 화면을 어려워하는 어르신은 종이로 푼다.
   지금은 상식 퀴즈를 뺀 여섯 게임이 모두 문제지를 만든다.
   (상식 퀴즈만 예외 — 보기를 고르고 시간을 재는 놀이라 종이에 옮길 것이 없다.)
   새 게임에는 `makeForPrint(level)` 을 갖추고 `js/print.js` 의 `SHEETS` 에 한 줄 더한다.
   그 둘만 갖추면 홈의 **「문제 출력」**(여러 종목을 섞어 뽑는 묶음)에도 저절로 낀다.
   문제지에는 시간·힌트·실수 같은 화면용 정보를 절대 넣지 않는다.
8. **디자인은 '건강 기록 스타일'이다** (`e087004` 에서 한지 톤에서 바뀌었다).
   흰 바탕 · 옅은 면(`--surf`)과 얇은 선(`--line`) · 초록 포인트 하나(`--accent`) · 그림자 없음.
   새 화면을 만들 때 색을 직접 적지 말고 `css/style.css` 맨 위의 이름(변수)을 쓴다.
9. **난이도는 5단계다** — 1 첫걸음 / 2 가볍게 / 3 쉬움 / 4 보통 / 5 어려움.
   보너스는 4단계 +100, 5단계 +250 이고 앞의 셋은 0이다. 시작 화면에는 `level__step` 으로 단계를 붙인다.
10. **화면에 나오는 글은 반드시 `T('...')` 로 감싼다.** 감싸지 않으면 영어판에서 한국어가 나온다.
    숫자가 끼어들면 `T('오늘 {n}종목', { n: n })` 처럼 자리를 판다. 이어 붙이면 안 된다.
    영어는 `js/lang/en.js` 에 채운다. 확인하는 법은 아래 '영어판 확인' 을 본다.
11. **일을 시작하기 전에 `main` 의 최신 상태에서 갈라져 나왔는지 먼저 확인한다.**
    (`git log --oneline -3 main`) 옛 시점에서 작업하면 디자인·구조가 어긋나 다시 만들게 된다.

## 코드 구조

```
index.html          화면 뼈대 · 스크립트 로딩 순서
css/style.css       전체 스타일 (건강 기록 스타일 · 인쇄용 스타일 포함)
js/storage.js       localStorage 한 곳 관리 (점수 · 설정 · 진행 중인 판)
js/i18n.js          말 바꾸기 — T('한국어') 를 사전에서 찾아 준다
js/lang/en.js       영어 사전 { '한국어': 'English' }
js/ui.js            공용 UI (토스트 · 모달 · 결과창 · 막대그래프 · 소리)
js/app.js           라우터 · 홈 · 기록 화면 · 설정
js/print.js         A4 문제지 인쇄
js/suggest.js       건의하기 — 서버 없이 호스팅(Netlify)이 받아 준다
css/page.css        검색용 낱장 페이지 전용 모양
js/analytics.js     방문자 세기 (구글 애널리틱스 · 측정 ID 를 넣어야 켜진다)
tools/build-seo.js  검색용 낱장 페이지·sitemap 만들기 (pack 이 먼저 돌린다)
tools/build-images.js 문제지 미리보기·카톡 공유 그림 찍기 (pack 이 함께 돌린다)
images/             위 도구가 만든 그림 — 손으로 고치지 않는다
js/data/            문제 은행 (퀴즈 · 낱말)
js/games/*.js       게임 한 개 = 파일 한 개
sw.js               오프라인 캐시
```

**게임 모듈 규약** — `window.Games.<id>` 에 아래를 그대로 갖춘 객체를 등록한다.

```js
{
  id, name, tagline,              // 게임 이름과 한 줄 설명
                                  // tagline 은 화면 맨 위 제목 밑에 나온다.
                                  // (한 글자 기호 icon 은 2026-08-21 에 없앴다 —
                                  //  종류가 늘수록 구별이 안 되고 이름 읽는 것을 방해했다)
  rules: { title, lines: [[항목, 설명], ...] },
  mount(container),               // 화면에 올라올 때
  unmount(),                      // 내려갈 때 — 타이머 정지 + 진행 상황 저장
  hasProgress(),                  // 이어서 할 판이 있는가
  art,                            // (있으면) 게임을 나타내는 작은 그림 —
                                  //  24×24 뷰박스의 SVG 속살만 (선은 currentColor 를 따른다).
                                  //  '어디로 갈까요?' 창에서 이름 옆에 나온다. 없으면 동그라미.
  langs,                          // (있으면) 이 말에서만 나온다 — 예: ['ko']
                                  //  낱말찾기·단어 순서처럼 그 나라 말이 있어야만
                                  //  되는 게임에만 적는다. 없으면 어느 말에서나 나온다.
                                  //  상식 퀴즈는 말마다 문제 은행을 갈아 끼우므로 적지 않는다.
  levels, levelOrder,             // 5단계 난이도 표와 그 순서
  makeForPrint(level)             // (있으면) 종이 문제지용 한 판 — 화면의 판은 건드리지 않는다
}
```

### 새 게임을 넣는 방법 (2026-08-21 부터 이렇게 줄었다)

1. `js/games/<id>.js` 를 만들어 `window.Games.<id>` 에 위 규약대로 등록한다.
2. `index.html` 에 `<script src="js/games/<id>.js"></script>` 한 줄을 더한다. **이게 전부다.**
3. (종이로도 풀 게임이면) `makeForPrint` 를 갖추고 `js/print.js` 의 `SHEETS` 에 한 줄 더한다.
4. `pack.cmd` 를 실행하면 버전이 저절로 올라가고 배포용 zip 이 만들어진다.

예전에 손대야 했던 곳들은 이제 스스로 따라온다.

- `js/app.js` 의 `GAMES` → `Object.keys(window.Games)` 로, **불러온 순서 그대로** 목록이 된다.
  화면에 보이는 순서를 바꾸려면 `index.html` 의 `<script>` 순서를 바꾼다.
- 화면 위 제목·설명 → 게임 파일의 `name` 과 `tagline` 을 읽어 쓴다.
- `js/storage.js` 의 `sessions` → 빈 목록에서 시작해 게임이 저장할 때 저절로 생긴다.
- `sw.js` 의 `ASSETS` → install 때 `index.html` 을 읽어 필요한 파일을 스스로 찾아낸다.
  (목록은 "최소한 이것만은"이라는 안전망으로만 남아 있다)
- 홈 · 게임 고르기 · 나의 기록 · 점수 규칙 · 게임 전환 화면 → 모두 `GAMES` 를 따라 저절로 늘어난다.

그 밖에 지킬 것
- 아래 탭은 **홈 · 게임 고르기 · 나의 기록** 셋으로 고정이다. 게임이 늘어도 탭은 늘리지 않는다.
- 점수는 **기본 1,000점 만점 + 난이도 보너스(보통 +100 / 어려움 +250)** 로 통일한다 (게임당 최대 1,250점).
- 판이 끝나면 `Store.addRecord({game, score, difficulty, duration, detail})` 로 기록한다.
- 진행 중인 판은 `Store.saveSession(id, data)` 로 저장하고, 날짜가 바뀌면 자동으로 사라진다.
- 코드 스타일은 기존 파일을 따른다: 빌드 도구 없는 순수 ES5풍 JS, IIFE 모듈, **주석은 한국어로**
  "왜 이렇게 했는지"를 남긴다.

## 영어판 확인

설정(⚙) → **말** 에서 English 를 고르면 화면이 통째로 다시 열린다.
(게임 이름·난이도 이름은 파일을 읽을 때 한 번만 정해지므로 다시 여는 것이다.)

영어에서는 `langs: ['ko']` 인 게임 둘(낱말찾기 · 단어 순서)이 목록에서 빠지고 다섯이 남는다 —
스도쿠 · 숫자 계산 · 상식 퀴즈 · 색칠 공부 · 틀린그림찾기.

상식 퀴즈는 말에 따라 문제 은행이 통째로 바뀐다 (`js/games/quiz.js` 의 `bank()`).
한국어는 `js/data/quiz-data.js`, 영어는 `js/data/quiz-data-en.js` 다.
분야 이름도 각 은행 안에 그 말로 들어 있으므로 `js/lang/en.js` 에 옮기지 않는다.
오답 노트와 이어서 하기도 말별로 갈라져 있어, 영어에서 한국어 문제가 나오지 않는다.

**문구를 고친 뒤에는 반드시 이 둘을 확인한다.**

1. 한국어로 두고 화면이 예전과 같은지 — `T('X')` 는 한국어일 때 `'X'` 를 그대로 돌려주므로
   제대로 감쌌다면 한 글자도 달라지지 않아야 한다.
2. 영어로 바꾸고 한국어가 남아 있지 않은지 — 남아 있으면 `js/lang/en.js` 에 그 문구가 없는 것이다.

## 개발용 실행

```bash
node server.js
```

`http://localhost:8123` 에서 열립니다.
